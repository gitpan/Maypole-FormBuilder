<div id="addnew">
<fieldset>
<legend>Add a new <% $request->model_class->moniker %></legend>
<% $request->as_form( mode   => 'addnew',
                      entity => $request->model_class,
                      reset  => 'reset',
                      submit => 'submit',
                      sticky => 0,
                      )->render %>
</fieldset>
</div>
