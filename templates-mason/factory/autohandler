<& header &>
<& title &> 
<& navbar &>

% $m->call_next;

<& footer &>

<%doc>
<%once>
    use Compress::Zlib;
</%once>
<%filter>
    if ($r->header_in( 'Accept-Encoding' ) =~ /((?:x-)?gzip)/ )
    {
        $r->content_encoding( $1 );
        $_ = Compress::Zlib::memGzip( $_ );
    } 
</%filter>
</%doc>
